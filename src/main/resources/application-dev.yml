server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mariadb://localhost:3306/first?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: org.mariadb.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.java17pro.demo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
